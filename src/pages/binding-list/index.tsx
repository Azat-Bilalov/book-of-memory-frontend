import React from "react";
import { BindingListWidget } from "@/widgets/binding-list-widget";

const BindingListPage: React.FC = () => {
  return (
    <div>
      <BindingListWidget />
    </div>
  );
};

export default BindingListPage;
