import React from "react";
import { BindingWidget } from "@/widgets/binding-widget";

const BindingPage: React.FC = () => {
  return (
    <div>
      <BindingWidget />
    </div>
  );
};

export default BindingPage;
